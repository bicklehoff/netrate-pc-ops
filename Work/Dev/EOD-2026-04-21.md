# EOD Handoff — 2026-04-21

**Device:** PC · **Department:** Dev · **Driver:** `zen-sammet-6cf8be` worktree
**Session length:** ~afternoon · **PR count:** 19 shipped
**MCP session id:** `cmo910o5ip57sut2f`

## TL;DR for next session

1. Run Startup Lite (get_briefing + check_relay REST API) — did not run at start today
2. Read this doc + MCP session log `cmo910o5ip57sut2f`
3. Default next move: **D9d batch** (ref_fha_ufmip → ref_hecm_config → ref_comp_caps). Unblocks D9b.10/11 and closes the biggest open audit thread. But check the Prioritized stack below first.
4. If David asks for something ad-hoc, the stack is fine to pause

## Today in one paragraph

Opened the afternoon on FoH April D9d PR 2 (retire county-limits.js server-side consumers — shipped as #136). Pivoted urgently when David discovered his MLO portal was broken while trying to send a lender letter to a borrower (Payam Kavousi). Three root causes surfaced: (1) loan wasn't in the DB because Zoho's corebot webhook had been silently dropping every incoming loan since at least Apr 17 due to a missing `loans.id` default; (2) prequal letter PDF had accumulated issues we'd never caught; (3) pipeline page had stale localStorage view hiding columns and the contacts endpoint was 500ing. Unblocked Payam via direct DB insert (loan `5088c8b2-09ad-4d6c-97bb-371b12cc1fb6`), then ran a 9-PR prequal polish cycle, then root-caused + shipped Migration 021 (id defaults on 7 tables) as a D4 schema-integrity follow-up. Introduced Tier A/B blast-radius tiering in the deploy skill mid-cycle (#141); 7 of 19 PRs shipped under Tier B with zero regressions.

## What shipped today (19 PRs)

**FoH April core:**
- #136 — D9d PR 2: retire county-loan-limits.js server consumers

**Prequal letter polish (9 PRs — MLO-only, all Tier B after #141):**
- #137 — PDF polish (logo border, spacing, formatting)
- #138 — headshot + trust-bar hierarchy + darker body text
- #139 — absolute photo URL + readable trust-bar labels
- #140 — SVG stars + force-refresh MLO fields on saved-state restore
- #141 — deploy skill Tier A/B + Google Reviews 4.9/5.0 revert
- #142 — `mloPhotoUrl` in `buildPdfData` bridge (root-cause of invisible photo)
- #143 — 44pt rounded-square headshot
- #145 — "Max Loan Amount" → "Loan Amount" label
- _(#144 is D4, not prequal)_

**D4 schema-integrity follow-up:**
- #144 — Migration 021 (7 ALTER SET DEFAULT `gen_random_uuid()`) + corebot ingest hotfix + audit doc amendment

**Meta:**
- #141 introduced Tier A/B deploy tiering

## Files touched (key ones)

```
prisma/migrations/021_id_defaults_gen_random_uuid.sql   NEW
scripts/_run-migration-021.mjs                          NEW
src/app/api/corebot/ingest/route.js                     +id = gen_random_uuid()
src/components/Portal/PrequalLetter/PrequalLetterPDF.js 10+ edits
src/components/Portal/PrequalLetter/PrequalLetterModal.js buildPdfData bridge fix
src/lib/rates/price-scenario.js                         DB DAL + derived FHA baseline
src/lib/quotes/eligibility.js                           DB DAL
src/lib/quotes/fee-builder.js                           hardcoded 726200 → DB baseline
src/lib/rates/defaults.js                               deleted FHA_BASELINE_LIMIT
.claude/skills/deploy/SKILL.md                          Tier A/B section
Work/Dev/audits/README.md                               D4 amended
```

## Prod state

- Last deploy: commit after #145 merge (2026-04-21 ~19:27 UTC), Vercel status Ready
- `main` branch caught up
- Worktree: `zen-sammet-6cf8be` on `fix/prequal-loan-amount-label` (can be reused or retired)
- Deploy lock: clean, no stale lock
- No rollback / hotfix pending

## Open items — prioritized for next session

### Sr dev pick: D9d batch (medium, high-value)

`ref_fha_ufmip`, `ref_hecm_config`, `ref_comp_caps`, `ref_va_funding_fee` — 4 new ref_* tables. Pattern is established from migration 020 (`ref_conforming_baselines` + `ref_county_loan_limits`). Each:

1. Migration with idempotent schema + seed
2. DAL in `src/lib/rates/ref-*.js` matching existing API surface
3. Consumer retirement follow-up PR

**Why this next:** closes biggest audit thread (D9d is 1/13 done), unblocks D9b.10 (`FHA_BASELINE_LIMIT` derivation) and D9b.11 (state closing-cost map → DB). Every static file is borrowing from the future.

### Tier B one-off: deploy skill — accept bare "Tier B"

Current SKILL.md says "deploy as tier B or /deploy --tier=B". David prefers just "Tier B". Update §Tier B section to match. 5-min PR, textbook Tier B.

### Parked for David's Zoho-side work: backfill Apr 17-21 gap

Query Zoho for records whose workflow-trigger field changed between Apr 17 and Apr 21 (likely `Modified_Time` filter on the stage that fires corebot). Reconcile against `loans.ldox_loan_id`. For missing ones: either re-save in Zoho (re-fires workflow, our receiver now works) or manually insert per the pattern we used for Payam. Only David can run the Zoho UI step; waiting on him to initiate.

### MLO Portal triage (medium, user-impacting)

Three bugs exposed by 2026-04-21 Payam incident, still live:

1. **Pipeline columns hidden** — saved `netrate_pipeline_state` localStorage predates recent column additions. Fix: add a "Reset to Default View" button that clears the key, or auto-merge new defaults on schema mismatch.
2. **Contacts 500** — `/api/portal/mlo/contacts` uses `sql(contactQuery, [params])` call-style (`src/app/api/portal/mlo/contacts/route.js:57`) which isn't compatible with `neon()`'s tagged-template API. Convert to tagged-template with dynamic ORDER BY via SQL-safe mapping.
3. **Prequal discoverability** — button is inside loan detail page, not a top-level nav. David expected a "Tools" item. UX debate, not a bug.

Suggest filing as **D10 — MLO Portal Exercise Audit** (user-flow scoped, not surface-scoped like D7).

### Tech debt swept into sight but not closed today

- **contacts.updated_at NOT NULL no default** — caught by migration 021 smoke test. Almost certainly more columns like this. Sweep `information_schema.columns` for NOT NULL no-default across public tables, fix in one migration.
- **Ingest endpoint observability** — Zoho ran silently broken for 4 days. No alert, no dashboard. Cheap fix: log every POST to `/api/corebot/ingest` + `/api/leads/inbound` + `/api/leads/form` to a small `ingest_log` table; daily cron alerts if 0 rows in 24h when >0 expected.

### Bigger queued items

- **D9c** — composable scenario/quote model (multi-day)
- **D9e** — application module architecture (multi-day, depends on D9c)
- **Backlog #79** — post-lead email sequence engine (multi-day, blocked on Claw copy via relay `mo8ugi59ji65junf`)
- **Backlog #88** — DSCR screening workflow
- **Font.register Inter** — fixes PDFelement "Helvetica corrupted" warning on the prequal letter (cosmetic)

## Key MCP references (search these instead of asking David)

- **Decision `cmo911n7t3doky0k5`** — Deploy skill Tier A/B tiering (why + scope + how)
- **Thought `cmo910web83yduvnj`** — 7 tables missing id defaults (class of bug)
- **Thought `cmo910wiybct9jme2`** — @react-pdf/renderer quirks (Image URL, stars, rgba)
- **Thought `cmo910ypj4xowsn1u`** — Zoho 200-with-errors ≠ failure in Zoho's log
- **Thought `cmo9113qtcf26wyou`** — MLO portal lacks audit dimension · D10 proposal

## Audit snapshot at EOD (v1.9+)

| Dim | Status |
|---|---|
| D1 Security | ✅ |
| D2 Case consistency | ✅ |
| D3 Pricing correctness | ✅ |
| D4 Data integrity | ✅ + schema follow-up shipped today (#144) |
| D5 Org scoping (half 1) | ✅ · half 2 absorbed into D9a |
| D6 Unified pricing/scenarios | ✅ |
| D7 MLO Portal UX | ✅ core · IA redesign deferred |
| D8 Static data inventory | ✅ inventory · remediations feed D9d |
| D9a Identity model | ✅ |
| D9b Pricing unification | 10/13 |
| D9c Scenario/quote model | 📋 spec only |
| D9d Reference data migration | 🟡 1/13 tables live |
| D9e Application modules | 📋 spec only |
| **D10 MLO Portal Exercise** | 🆕 proposed — not yet filed |

## Things NOT to do on reopen

- Don't push the current `fix/prequal-loan-amount-label` branch again (#145 already merged)
- Don't run migration 021 again (applied to prod; idempotent but wasteful)
- Don't manually insert Payam's loan again (already exists: `5088c8b2-09ad-4d6c-97bb-371b12cc1fb6`)
- Don't attempt Zoho backfill without David — needs his UI access

## One question to ask David on next session

> "Audit status update says D9d is 1/13 tables live. Pick 4 tables for next batch (I'd default to fha_ufmip → hecm_config → comp_caps → va_funding_fee) or pivot to D10 portal exercise audit first?"
