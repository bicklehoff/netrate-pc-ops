export const metadata = {
  title: 'Book a Call | NetRate Mortgage',
  description: 'Schedule a free consultation with a NetRate Mortgage loan officer. Pick a time that works for you.',
  alternates: {
    canonical: 'https://www.netratemortgage.com/book',
  },
};

export default function BookLayout({ children }) {
  return children;
}
