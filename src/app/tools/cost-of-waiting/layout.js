export const metadata = {
  title: 'Cost of Waiting Calculator | NetRate Mortgage',
  description: 'See how much waiting to refinance or buy costs you each month. Compare scenarios side by side.',
  alternates: { canonical: 'https://netratemortgage.com/tools/cost-of-waiting' },
};

export default function CostOfWaitingLayout({ children }) {
  return children;
}
