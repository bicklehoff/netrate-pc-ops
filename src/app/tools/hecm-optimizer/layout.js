export const metadata = {
  title: 'HECM Optimizer | NetRate Mortgage',
  description: 'Optimize your HECM reverse mortgage options. Redirects to the reverse mortgage calculator.',
  alternates: { canonical: 'https://www.netratemortgage.com/tools/hecm-optimizer' },
};

export default function HecmOptimizerLayout({ children }) {
  return children;
}
